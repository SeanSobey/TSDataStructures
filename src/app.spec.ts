import * as assert from 'assert';

describe('example', () => {
	it('passes', () => {
		assert.equal(true, true);
	});
	it('passes again', () => {
		assert.equal(true, true);
	});
	it('passes again again', () => {
		assert.equal(true, true);
	});
});